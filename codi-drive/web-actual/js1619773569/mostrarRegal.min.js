var marginTop;
var domini = "https://www.prisma.cat/";

function mostrarHeaderFooter() {
   var mostraHeader = $.ajax({
   	url: domini + "ajax/mostrar_header_2.php",
   	method: "GET",
   	dataType: "html"
   });
   mostraHeader.done(function(page) {
   		$("header").html(page);
   		$('.closebtn').css('display', 'none');
   		if (/Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent))
   			$('.navbar-nav > li').addClass('mobile');
   		else
   			$('.navbar-nav > li').addClass('computer')

   		$('.prisma-header').on('focus', '.form-control', function(){
   			$(this).next().next().addClass('active');
   		});
   		$('.prisma-header').on('blur', '.form-control', function(){
   			if ($(this).val()=='')
   				$(this).next().next().removeClass('active');
   		});
   });
   mostraHeader.fail(function(jqXHR, textStatus, errorThrown) {
   	rerrorFunction(jqXHR, textStatus, errorThrown, "Hi ha hagut algun error a l'hora de carregar la capçalera de pàgina");
   });

   var mostraFooter = $.ajax({
   	url: domini + "ajax/mostrar_footer_2.php",
   	method: "GET",
   	dataType: "html"
   });
   mostraFooter.done(function(page) {
   		$("footer").html(page);

			adjustStyle();

			$(window).resize(function() {
				adjustStyle();
			});
   		$('.form-footer').on('focus', '.form-control', function(){
   			$('.form-footer label').hide();
   		});
   		$('.form-footer').on('blur', '.form-control', function(){
   			if ($('#adreca-electronica').val()=='')
   				$('.form-footer label').show();
   		});
   		function mostrarErrorButlleti(missatgeError) {
   			$('#modalErrorBody').html(missatgeError);
   			$('#modalError').modal('show')
   		}
   		function mostrarSuccessButlleti(missatgeSuccess) {
   			$('#modalOkBody').html(missatgeSuccess);
   			$('#modalOK').modal('show');
   		}
   		function validacioCorreuButlleti() {
   			var email = $('#adreca-electronica').val();
   			var error = "";
   			if (email.length != 0) {
   				var tfld_email = $.trim(email);
   				var emailFilter = /^([\w-\.]+@([\w-]+\.)+[\w-]{2,4})?$/;
   				var illegalChars = /[\(\)\<\>\,\;\:\\\"\[\]]/;
   				if (!emailFilter.test(tfld_email)) {
   					error = "El correu electrònic no és vàlid";
   				} else if (email.match(illegalChars)) {
   					error = "El correu electrònic té caràcters no permesos";
   				}
   			}
   			else
   				error = "Cal omplir el camp";
   			return error;
   		}
   		$('.form-footer').on('click', '#news', function(){
   			if (validacioCorreuButlleti() != '') {
   				var missatgeError = "<p>Cal omplir el camp de l'adreça electrònica.</p>";
   				mostrarErrorButlleti(missatgeError);
   			}
   			else {
   				var correu = $('#adreca-electronica').val();
   				var comprovaSpam = $("#butlletiSpam").val();
   				var mailingNouUser = $.ajax({
   					url: domini + "ajax/mailingNou.php",
   					method: "GET",
   					data: {
   						correu: correu,
   						comprova: comprovaSpam
   					},
   					dataType: "html"
   				});
   				mailingNouUser.done(function(message) {
   					if (!message.toLowerCase().includes("error")) {
   						var missatgeConsultaOK = "<p>T'has subscrit correctament al nostre butlletí electrònic.</p>";
   						missatgeConsultaOK += "<p>En breu rebràs un missatge de confirmació en del correu ";
   						missatgeConsultaOK += "<strong><span class='correu_consulta'>" + correu + "</span></strong>.</p>";
   						missatgeConsultaOK += "<p>Si el missatge no arriba en 15 minuts, revisa la carpeta del correu brossa. ";
   						missatgeConsultaOK += "<p>Gr&agrave;cies per confiar en PrisMa!</p>";
   						mostrarSuccessButlleti(missatgeConsultaOK)
   					}
   					else {
   						mostrarErrorButlleti(message)
   					}
   				});
   				mailingNouUser.fail(function(jqXHR, textStatus, errorThrown) {
   					rerrorFunction(jqXHR, textStatus, errorThrown, "Hi ha hagut algun error a l'hora de carregar el tastet");
   				});
   			}
   		});
   		function checkAcceptCookies() {
   			if(localStorage.getItem("acceptCookies") == 'true'){
   				$('#cntCookies').hide();
   			}
   		}
   		checkAcceptCookies();
   });
   mostraFooter.fail(function(jqXHR, textStatus, errorThrown) {
   	rerrorFunction(jqXHR, textStatus, errorThrown, "Hi ha hagut algun error a l'hora de carregar el peu de pàgina");
   });
}
function acceptCookies() {
	localStorage.setItem("acceptCookies", "true");
	$('#cntCookies').slideUp();
	consentGrantedAdStorage();
	consentGrantedAdUserData();
	consentGrantedAdPersonalization();
	consentGrantedAnalyticsStorage();
}
mostrarHeaderFooter();
var urlPagina = window.location.pathname.split('?')[0];
if (urlPagina.substr(-1) == "/") urlPagina = urlPagina.substr(0, urlPagina.length - 1);
var dispositiu;
if (/Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent)) {
   dispositiu = "mobil";
   $('#boto-tancar').css('display', 'none');
   $('#top-menu').remove()
} else {
   dispositiu = "ordinador"
}

let pagina = 1, //ens indica que estem a la pàgina del regala un curs
	hemPassatPerLaPagina1 = false;
	codiCurs = '', //ens indica el curs que estem regalant
	formAfort_PerQqui = '',
	formAfort_DeQui = '',
	formAfort_Dedicatoria = '',
   formAfort_DedicatoriaMa = '',
	codiRegal = '',
	estilRegal = '',
   documentacio = '',
   preu = 0,
	percentatge = 0,
   nomCurs = '',
   hores = 0,
   codiPostal = '';

let screenWidth = parseInt($(this).width());

/* ############################### FUNCIONALITATS ############################### */

function mostrarPagina() {
   removeEvents();
	if (pagina == 1) { //mostra la pàgina del regala un curs
		var mostraPageRegal = $.ajax({
			url: domini + "ajax/mostrar_pagina_regal.php",
			method: "GET",
			dataType: "html"
		});

		mostraPageRegal.done(function(page) {
			hemPassatPerLaPagina1 = true;
			$("#contingut").html(page);

			$('#cnt-cursos').on('click', '.curs-regal-overlay', function() {
				regalaCurs( $(this).parent().attr('id').split('-')[2] );
			});
			$('#cnt-cursos').on('click', '.curs-regal-titol', function() {
				regalaCurs( $(this).parent().attr('id').split('-')[2] );
			});
		});

		mostraPageRegal.fail(function(jqXHR, textStatus, errorThrown) {
			rerrorFunction(jqXHR, textStatus, errorThrown, "Hi ha hagut algun error a l'hora de carregar la pàgina");
		});

	}
	else if (pagina == 2) { //mostra el formulari de la targeta regal
      //obtenir preu de la pròxima edició del codi curs
		var requestPreuHoresNomCurs = $.ajax({
			url: domini + "ajax/obtenirPreuHoresNomCursRegal.php",
			method: "GET",
			data: {
				codi: codiCurs
			},
			dataType: "html"
		});
		requestPreuHoresNomCurs.done(function(preuHoresNomCurs) {
			if (!preuHoresNomCurs.toLowerCase().includes("error")) {
				vectpreuHoresNomCurs = preuHoresNomCurs.split('|');
				preu = parseFloat(vectpreuHoresNomCurs[0]);
				hores = parseInt(vectpreuHoresNomCurs[1]);
				nomCurs = vectpreuHoresNomCurs[2];
				percentatge = vectpreuHoresNomCurs[3];
				//Consulta per mostrar el formulari de l'afortunat
				var request = $.ajax({
					url: domini + "ajax/mostrar_formulari_afortunat_regal.php",
					method: "GET",
					data: {
						codi: codiCurs,
						desti: formAfort_PerQqui,
						origen: formAfort_DeQui,
						dedicatoria: formAfort_Dedicatoria,
						hores: hores,
						preu: preu,
						percentatge: percentatge
					},
					dataType: "html"
				});

				request.done(function(page) {
					if (!page.toLowerCase().includes("error")) {
						$(".cntPage").html(page);
						if (hemPassatPerLaPagina1) {
							$(".cntPage").fadeIn("slow");
							$('html, body').animate({
								scrollTop: 0
							}, 'slow');
						}
						//Afegeix i elimina la classe "active" del input que s'ha clicat a sobre seu
						$('.form-group').on('focus', '.form-control', function() {
							$(this).prev().addClass('active');
						});
						$('.form-group').on('blur', '.form-control', function() {
							if ($(this).val().trim() == '')
								$(this).prev().removeClass('active');
						});
						//Valida el input "desti" en cas que es modifiqui o es cliqui sobre seu
						$('.cntPage').on('change', '#desti', function() {
							validarDesti()
						});
						$('.cntPage').on('blur', '#desti', function() {
							validarDesti()
						});
						$('.cntPage').on('focus', '#desti', function() {
							eliminarError('desti_erroni')
						});
						if (formAfort_PerQqui != '') $('#desti').prev().addClass('active');
						else $('#desti').prev().removeClass('active');
						if (formAfort_DeQui != '') $('#origen').prev().addClass('active');
						else $('#origen').prev().removeClass('active');
						//Quan es clica l'element amb id «enrere», anem a la pàgina anterior
						$('.cntPage').on('click', '#enrereForm', function() {
							pagina--;
							mostrarPagina();
						});
						//Quan es clica l'element amb id continua, validem l'input origen i l'input dedicatoria.
						//Si no hi ha cap error, anem a la següent pàgina;
						//Si hi ha algun error, mostrem un modal amb l'error.
						$('.cntPage').on('click', '#continuaForm', function() {
							var comprovacio = '',
								validDesti = validarDesti(),
								validDedicatoria = '';

							if (validDesti.length != 0) comprovacio += "<li>Per a qui</li>";

							if (comprovacio != "") {
								var missatgeError = "<p>El camp següent és incorrecte:</p>";
								missatgeError += "<ul class='errors'>" + comprovacio + "</ul>";
								mostrarModalError(missatgeError);
							} else {
								$("#modalLoading").modal('show');
								pagina++;
								formAfort_PerQqui = $('#desti').val().trim();
								formAfort_DeQui = $('#origen').val().trim();
								formAfort_Dedicatoria = $('#dedicatoria').val().trim();
								formAfort_DedicatoriaMa = '',
									codiRegal = '',
									estilRegal = '';
								if ($('#dedicatoria_ma').is(':checked'))
									formAfort_DedicatoriaMa = 'checked';
								mostrarPagina();
							}
						});
					}
				});

				request.fail(function(jqXHR, textStatus, errorThrown) {
					rerrorFunction(jqXHR, textStatus, errorThrown, "Hi ha hagut algun error a l'hora de mostrar el forumulari de l'afortunat");
				});

			}
		});
		requestPreuHoresNomCurs.fail(function(jqXHR, textStatus, errorThrown) {
			rerrorFunction(jqXHR, textStatus, errorThrown, "Hi ha hagut algun error a l'hora d'obtenir les dades del curs regal");
		});
	}
	else if (pagina == 3) { //previsualitza el regal
		var prevRegal = $.ajax({
			url: domini + "ajax/previsualitza_regal.php",
			method: "GET",
			data: {
				codi: codiCurs,
				estil: estilRegal,
				codiRegal: codiRegal,
				desti: formAfort_PerQqui,
				origen: formAfort_DeQui,
				dedicatoria: formAfort_Dedicatoria,
				dispositiu: dispositiu
			},
			dataType: "html"
		});
		prevRegal.done(function(page) {
			if (!page.toLowerCase().includes("error")) {
				$("#modalLoading").modal('hide');
				$(".cntPage").html(page);
				$('#estil-4').addClass('marcat');

				$('.cntPage').on('click', '.estil', function() {
					//canviem l'estil
					$('.estil').removeClass('marcat');
					$(".estil").each(function(){
						 $('.regalar-curs').removeClass($(this).attr('id'));
					})
					$(this).addClass('marcat');
					$('.regalar-curs').addClass($(this).attr('id'));
					$('.val-regal').attr('src','https://www.prisma.cat/img/regal/val-regal-'+$(this).attr('id')+'.jpg');
					$('.val-regal-webp').attr('srcset','https://www.prisma.cat/img/regal/val-regal-'+$(this).attr('id')+'.webp');
					$('.val-regal-webp').attr('data-srcset','https://www.prisma.cat/img/regal/val-regal-'+$(this).attr('id')+'.webp');
					$('.val-regal-jpg').attr('srcset','https://www.prisma.cat/img/regal/val-regal-'+$(this).attr('id')+'.jpg');
					$('.val-regal-jpg').attr('data-srcset','https://www.prisma.cat/img/regal/val-regal-'+$(this).attr('id')+'.jpg');
				});
				//Quan es clica l'element amb id «enrere», anem a la pàgina anterior
				$('.cntPage').on('click', '#enrerePrev', function() {
					pagina--;
					mostrarPagina();
				});
				//Quan es clica l'element amb id continua, anem a la següent pàgina;
				$('.cntPage').on('click', '#continuaPrev', function() {
					$("#modalLoading").modal('show');
					codiRegal = $('.cnt-codi-regal').html();
					estilRegal = $('.estil.marcat').attr('id');
					pagina++;
					mostrarPagina();
				});
			}
		});
		prevRegal.fail(function(jqXHR, textStatus, errorThrown) {
			rerrorFunction(jqXHR, textStatus, errorThrown, "Hi ha hagut algun error a l'hora de Previsualitzar el regal");
		});

	}
   else if (pagina == 4) {
		var mostraFormComp = $.ajax({
			url: domini + "ajax/mostrar_formulari_comprador.php",
			method: "GET",
			dataType: "html"
		});
		mostraFormComp.done(function(page) {
			if (!page.toLowerCase().includes("error")) {
				$("#modalLoading").modal('hide');
				$(".cntPage").html(page);

				//Afegeix i elimina la classe "active" del input que s'ha clicat a sobre seu
				$('.form-group').on('focus', '.form-control', function() {
					$(this).prev().addClass('active');
				});
				$('.form-group').on('blur', '.form-control', function() {
					if ($(this).val().trim() == '')
						$(this).prev().removeClass('active');
				});

				mostrarDocumentacio('doc-dni');

				$(".form-group .select").click(function(e) {
					var lista = $(this).find("ul"),
						triangle = $(this).find("i");
					e.preventDefault();
					$(this).find("ul").toggle();
					if (lista.is(":hidden")) {
						triangle.removeClass("fa-angle-up").addClass("fa-angle-down");
					} else {
						triangle.removeClass("fa-angle-down").addClass("fa-angle-up");
					}
				});

				$('.cntPage').on('change', '#nom', function() {
					validarNom()
				});
				$('.cntPage').on('blur', '#nom', function() {
					validarNom()
				});
				$('.cntPage').on('focus', '#nom', function() {
					eliminarError('nom_cognom_erroni')
				});

				$('.cntPage').on('change', '#cog', function() {
					validarCognoms()
				});
				$('.cntPage').on('blur', '#cog', function() {
					validarCognoms()
				});
				$('.cntPage').on('focus', '#cog', function() {
					eliminarError('cognom_erroni')
				});

				$(".form-group #doc").on("click", "li", function(e) {
					var texto = $(this).text(),
						element = $(this).parent().prev(),
						lista = $(this).closest("ul"),
						triangle = $(this).parent().next(),
						id = $(this).attr('id');
					e.preventDefault();
					e.stopPropagation();
					element.text(texto);
					lista.hide();
					triangle.removeClass("fa-angle-up").addClass("fa-angle-down");
					mostrarDocumentacio(id);
				});

				$('.cntPage').on('change', '#telf', function() {
					validarTel()
				});
				$('.cntPage').on('blur', '#telf', function() {
					validarTel()
				});
				$('.cntPage').on('focus', '#telf', function() {
					eliminarError('telf_erroni')
				});

				$('.cntPage').on('change', '#email', function() {
					validarEmail(0)
				});
				$('.cntPage').on('blur', '#email', function() {
					validarEmail(0)
				});
				$('.cntPage').on('focus', '#email', function() {
					eliminarError('correu_erroni')
				});

				$('.cntPage').on('change', '#email_conf', function() {
					validarEmailConf(0)
				});
				$('.cntPage').on('blur', '#email_conf', function() {
					validarEmailConf(0)
				});
				$('.cntPage').on('focus', '#email_conf', function() {
					eliminarError('correu_conf_erroni')
				});

				$('.cntPage').on('change', '#adreca', function() {
					validarAdreca()
				});
				$('.cntPage').on('blur', '#adreca', function() {
					validarAdreca()
				});
				$('.cntPage').on('focus', '#adreca', function() {
					eliminarError('adreca_erroni')
				});

				$('.cntPage').on('change', '#cp', function() {
					validarCP()
				});
				$('.cntPage').on('blur', '#cp', function() {
					mostrarLlistatPoblacions();
					validarCP()
				});
				$('.cntPage').on('focus', '#cp', function() {
					eliminarError('cp_erroni')
				});

				$('.cntPage').on('change', '#poble', function() {
					validarPoble()
				});
				$('.cntPage').on('blur', '#poble', function() {
					validarPoble()
				});
				$('.cntPage').on('focus', '#poble', function() {
					eliminarError('poble_erroni')
				});

				//Quan es clica l'element amb id «enrere», anem a la pàgina anterior
				$('.cntPage').on('click', '#enrereFormCompr', function() {
					pagina--;
					mostrarPagina();
				});

				//Quan es clica l'element amb id continuaFormCompr, s'envia la inscripció
				$(".cntPage").on('click', '#continuaFormCompr', function() {
					var comprovacio = '',
						validDoc = '',
						validNom = validarNom(),
						validCognom = validarCognoms(),
						validTel = validarTel(),
						validEmailNoBuit = validarEmail(1),
						validEmailConfNoBuit = validarEmailConf(1),
                  validEmailCorreu = correuAdvert(1),
						validAdreca = validarAdreca(),
						validCP = validarCP(),
						validPoble = validarPoble();

					if ($("#doc .element-selected").html().trim() == 'NIF/NIE')
						validDoc = validarNif();
					else
						validDoc = validarPass();

					if (validNom.length != 0) comprovacio += "<li>Nom</li>";
					if (validCognom.length != 0) comprovacio += "<li>Cognoms</li>";
					if ($("#doc .element-selected").html().trim() == 'NIF/NIE') {
						if (validDoc.length != 0) comprovacio += "<li>Dni sense lletra</li>";
					} else {
						if (validDoc.length != 0) comprovacio += "<li>Número de passaport</li>";
					}
					if (validTel.length != 0) comprovacio += "<li>Telèfon</li>";
               if (validEmailNoBuit.length != 0 || validEmailConfNoBuit.length != 0 || validEmailCorreu.length != 0 ) comprovacio += "<li>Correu electrònic</li>";
					if (validAdreca.length != 0) comprovacio += "<li>Adreça postal</li>";
					if (validCP.length != 0) comprovacio += "<li>Codi postal</li>";
					if (validPoble.length != 0) comprovacio += "<li>Població</li>";

					if (comprovacio != "") {
						var missatgeError = "<p>Els camps següents són incorrectes:</p>";
						missatgeError += "<ul class='errors'>" + comprovacio + "</ul>";
						mostrarModalError(missatgeError);
					} else {
						$("#modalLoading").modal('show');
						enviarInscripcioRegal()
					}
				});
			}
		});
		mostraFormComp.fail(function(jqXHR, textStatus, errorThrown) {
			rerrorFunction(jqXHR, textStatus, errorThrown, "Hi ha hagut algun error a l'hora de mostrar el forumlari del comprador");
		});
	}
}

if (urlPagina.split('/')[2]) {
	pagina = 2; //ens indica que estem a la pàgina del regala un curs seleccionat
	//crida ajax per obtenir el codi del curs
	codiCurs = '';
	var requestCodiCurs = $.ajax({
		url: domini + "ajax/obtenirCodiCurs.php",
		method: "GET",
		data: {
			url: urlPagina
		},
		dataType: "html"
	});
	requestCodiCurs.done(function(codi_curs) {
		if (!codi_curs.toLowerCase().includes("error")) {
			codiCurs = codi_curs;
			var mostraPageRegal = $.ajax({
				url: domini + "ajax/mostrar_pagina_regal.php",
				method: "GET",
				dataType: "html"
			});
			mostraPageRegal.done(function(page) {
					$("#contingut").html(page);
					$(".cntPage").html();
					mostrarPagina();
			});
			mostraPageRegal.fail(function(jqXHR, textStatus, errorThrown) {
				rerrorFunction(jqXHR, textStatus, errorThrown, "Hi ha hagut algun error a l'hora de mostrar la pàgina del regal");
			});
		}
	});
	requestCodiCurs.fail(function(jqXHR, textStatus, errorThrown) {
		rerrorFunction(jqXHR, textStatus, errorThrown, "Hi ha hagut algun error a l'hora d'obtenir el codi del curs");
	});

}
else {
   mostrarPagina();
}

//Mostra els cursos de X hores
function mostraCursos(hores) {
	$('#cnt-hores button').removeClass('marcat');
	$('#hores-' + hores).addClass('marcat');
	$("#modalCercantCursos").modal('show');
	var mostraCursosHores = $.ajax({
		url: domini + "ajax/mostrar_cursos_regal.php",
		method: "GET",
		data: {
			hores: hores,
			dispositiu: dispositiu
		},
		dataType: "html"
	});
	mostraCursosHores.done(function(cntCursos) {
		if (!cntCursos.toLowerCase().includes("error")) {
			$("#cnt-cursos").html(cntCursos);
			$("#cnt-hores").addClass('mb-4 border-bottom');
			$("#modalCercantCursos").modal('hide');
		}
	});
	mostraCursosHores.fail(function(jqXHR, textStatus, errorThrown) {
		rerrorFunction(jqXHR, textStatus, errorThrown, "Hi ha hagut algun error a l'hora de mostrar els cursos del regal");
	});

}

//Desapareix el contingut anterior i mostra la pàgina del formulari de la targeta regal
function regalaCurs(curs) {
	$(".cntPage").fadeOut("fast", function() {
		pagina++;
		codiCurs = curs;
		mostrarPagina();
	});
}
function mostraInfoCurs(link) {
	window.open(link, '_blank');
}

//Mostra un tastet del curs "codi"
function mostrarTaset(codi) {
	if (!$('#veure_tastet_' + codi)[0]) {
		var reqTastet = $.ajax({
			url: domini + "ajax/mostrar_tastet.php",
			method: "GET",
			data: {
				dispositiu: dispositiu,
				codi: codi
			},
			dataType: "html"
		});

		reqTastet.done(function(tastet) {
			if (!tastet.toLowerCase().includes("error")) {
				$("#contingut").append(tastet);
				//Mostrar el video de la presentacio del curs quan cliques la caratula
				var video = $('#veure_tastet_' + codi + " .video-responsive");
				video.on("click", function() {
					var obj = $(this);
					$(this).find('iframe')[0].src = $(this).find('iframe')[0].src.replace("autoplay=0", "autoplay=1");
					setTimeout(function() {
						obj.find('button').hide();
						obj.find('img').hide();
					}, 500);
				});
				//Pausar el video quan surts del modal de veure un tastet
				$('#veure_tastet_' + codi).on('hide.bs.modal', function(e) {
					if ($(this).find('iframe')[0]) {
						var src = $(this).find('iframe')[0].src;
						$(this).find('iframe')[0].src = src;
						$(this).find('iframe')[0].src = src.replace("autoplay=1", "autoplay=0");
						$(this).find('img').show();
						$(this).find('button').show();
					}
				});
				$('#veure_tastet_' + codi).modal('show');
			}
		});

		reqTastet.fail(function(jqXHR, textStatus, errorThrown) {
			rerrorFunction(jqXHR, textStatus, errorThrown, "Hi ha hagut algun error a l'hora de mostrar el tastet");
		});

	} else {
		$('#veure_tastet_' + codi).modal('show');
	}

}

function enviarInscripcioRegal() {
	//enviem inscripció
	//si no hi ha cap error, carrego la pàgina del pagament
	//si hi ha algun error, mostra el modal d'error

   var nom = $("#nom").val().trim(),
		 cog = $("#cog").val().trim(),
		 telf = $("#telf").val().trim(),
		 email = $("#email").val().trim(),
		 adreca = $("#adreca").val().trim(),
		 codiPostal = $("#cp").val().trim(),
		 poblacio = $("#poble").val().trim();
		 comentaris = $("#comentaris").val().trim();

	var sendInsc = $.ajax({
		url: domini + "ajax/enviarInscripcioRegal.php",
		method: "GET",
		data: {
			nom: nom,
			cog: cog,
			dni: documentacio,
			telf: telf,
			email: email,
			adreca: adreca,
			codiPostal: codiPostal,
			poblacio: poblacio,
			comentaris: comentaris,
			codiCurs: codiCurs,
			nomCurs: nomCurs,
			preu: preu,
			percentatge: percentatge,
			hores: hores,
			codiRegal: codiRegal,
			estilRegal: estilRegal,
			desti: formAfort_PerQqui,
			origen: formAfort_DeQui,
			dedicatoria: formAfort_Dedicatoria
		},
		dataType: "html"
	});
	sendInsc.done(function(msgInsc) {
		if (!msgInsc.toLowerCase().includes("error")) {
			var urlConf = domini + "regal/confirmacio/" + msgInsc;
			window.location.replace(urlConf);
		}
		else {
			$('#modalLoading').modal('hide');
			mostrarModalError(msgInsc );
		}
	});
	sendInsc.fail(function(jqXHR, textStatus, errorThrown) {
		rerrorFunction(jqXHR, textStatus, errorThrown, "Hi ha hagut algun error a l'hora d'enviar la inscripció");
	});
}

function mostrarDocumentacio(id) {
	var nomCamp = 'DNI amb lletra';
	var idInput = 'nif';
	var idError = 'dni_erroni';
	var input = returnInputType1(nomCamp, idInput, idError);
	$('#input_doc').html(input);

   $('#input_doc').off();

	$('#input_doc').on('focus', '#nif', function() {
      $(this).prev().addClass('active');
   });
   $('#input_doc').on('blur', '#nif', function() {
      if ($(this).val().trim() == '')
         $(this).prev().removeClass('active');
   });

	$('.cntPage').on('change', '#' + idInput, function() {
		validarNif();
		documentacio = $('#nif').val().trim();
	});
	$('.cntPage').on('blur', '#' + idInput, function() {
		validarNif();
		documentacio = $('#nif').val().trim();
	});
	$('.cntPage').on('focus', '#' + idInput, function() {
		eliminarError(idError)
	});

	if (id == 'doc-passaport') {
		var nomCamp = 'Número de passaport';
		var idInput = 'pass';
		var idError = 'dni_erroni';
		var input = returnInputType1(nomCamp, idInput, idError);
		$('#input_doc').html(input);

      $('#input_doc').on('focus', '#pass', function() {
         $(this).prev().addClass('active');
      });
      $('#input_doc').on('blur', '#pass', function() {
         if ($(this).val().trim() == '')
            $(this).prev().removeClass('active');
      });

		$('#' + idInput).focus();

		$('.cntPage').on('change', '#' + idInput, function() {
			validarPass();
			documentacio = $('#pass').val().trim();
		});
		$('.cntPage').on('blur', '#' + idInput, function() {
			validarPass();
			documentacio = $('#pass').val().trim();
		});
		$('.cntPage').on('focus', '#' + idInput, function() {
			eliminarError(idError)
		});
	}
}

function returnInputType1(nomCamp, idInput, idError) {
	var input = "<div class='form-group field-wrap position-relative'>";
	input += "<label class='position-absolute mb-0'>";
	input += "<span class='camp'>" + nomCamp + "</span>";
	input += "<span class='req'>*</span>";
	input += "</label>";
	input += "<input type='text' class='form-control' id='" + idInput + "' name='" + idInput + "'>";
	input += "<span id='" + idError + "' class='d-flex justify-content-center align-items-center px-2 position-absolute text-center text-white'></span></div>";
	return input;
}

function mostrarLlistatPoblacions() {
	if (validarCP().length == 0) {
		var cp = $("#cp").val().trim();
		$("#llistat_poblacions").show();

		if (codiPostal != cp) {
			var reqPobl = $.ajax({
				url: domini + "ajax/buscarPoblacio.php",
				method: "GET",
				data: {
					cp: cp
				},
				dataType: "html"
			});
			reqPobl.done(function(llistat) {
				if (!llistat.toLowerCase().includes("error")) {
					$("#llistat_poblacions").html(llistat);
					codiPostal = cp;
					$("#llistat_poblacions").show();

					$(".cnt-poble").on("click", "li", function(e) {
						var texto = $(this).text(),
							element = $(this).parent().prev(),
							lista = $(this).closest("ul"),
							triangle = $(this).parent().next(),
							id = $(this).attr('id');
						e.preventDefault();
						e.stopPropagation();
						lista.hide();
						var valorPoble = $('#' + id + " a").html().trim();
						$('#poble').val(valorPoble);
						$('.cnt-poble label').addClass('active');
					});
				}
			});
			reqPobl.fail(function(jqXHR, textStatus, errorThrown) {
				rerrorFunction(jqXHR, textStatus, errorThrown, "Hi ha hagut algun error a l'hora de buscar la població");
			});

		}
	}
}

function removeEvents() {
	$('.form-group').off();
	$('.cntPage').off();
	$(".form-group #doc").off();
	$(".form-group .cnt-poble").off();
}

/* ############################### VALIDACIONS ############################### */

//Comprova si el input amb id "desti", no és buit.
//Si està buit, apareix l'error "El camp està buit" a l'element amb id "desti_erroni"
function validarDesti() {
	var error = validacioCampBuit("desti", "desti_erroni");
	return error;
}

//Comprova si el input amb id "dedicatoria", no és buit sempre i quan el input amb id "dedicatoria_ma" no està marcat.
//Si està buit, apareix l'error "El camp està buit" a l'element amb id "dedicatoria_erroni"
function validarDedicatoria() {
	var error;
	if (!$('#dedicatoria_ma').is(':checked'))
		error = validacioCampBuit("dedicatoria", "dedicatoria_erroni");
	return error;
}

function validarNom() {
	var error = validacioCampBuit("nom", "nom_cognom_erroni");
	if (error.length == 0) error = validarNomDiferentCognom();
	return error;
}

function validarCognoms() {
	var error = validacioCampBuit("cog", "cognom_erroni");
	if (error.length == 0) error = validarNomDiferentCognom();
	return error;
}

function validarNomDiferentCognom() {
	var nom = $('#nom').val().trim();
	var cog = $('#cog').val().trim();
	var error = "";

	if (nom.length != 0 && cog.length != 0) {
		if (cog == nom) {
			error = "El nom i els cognoms no poden coincidir";
			mostrarError("nom_cognom_erroni", error);
		} else
			eliminarError("nom_cognom_erroni");
	}
	return error;
}

function validarNif() {
	var nif = $('#nif').val().trim();
	var error = "";

	var error = validacioCampBuit("nif", "dni_erroni");;

	if (nif.length != 0) {
		var stripped = nif.replace(/[\(\)\.\-\ ]/g, '');

		if (!(stripped.length == 9)) {
			error = "Llargada incorrecta";
			mostrarError("dni_erroni", error);
		} else {
			var primera_posicio = nif.charAt(0);
			if (primera_posicio == "x" || primera_posicio == "y" || primera_posicio == "X" || primera_posicio == "Y") {
				numeros = nif.split(primera_posicio);
				if (!esNumeric(numeros[1])) {
					error = "NIF/NIE erroni";
					mostrarError("dni_erroni", error);
				}
			} else if (!(esNumeric(nif))) {
				error = "NIF/NIE erroni";
				mostrarError("dni_erroni", error);
			}
		}
	}
	if (error.length == 0) {
		var lletra = nif.charAt(8);
		if (!(esAlphabetic(lletra))) {
			error = "NIF/NIE erroni";
			mostrarError("dni_erroni", error);
		} else {
			var lockup = 'TRWAGMYFPDXBNJZSQVHLCKE';
			dni = nif.substring(0, 8);

			primer = dni.charAt(0);
			resta = dni.substring(1);

			if (primer.toUpperCase() == "X")
				dni = 0 + resta;
			else if (primer.toUpperCase() == "Y")
				dni = 1 + resta;
			else if (primer.toUpperCase() == "Z")
				dni = 2 + resta;

			correcta = lockup.charAt(dni % 23);

			if (lletra.toUpperCase() != correcta) {
				error = "La lletra no correspon";
				mostrarError("dni_erroni", error);
			} else
				eliminarError("dni_erroni");
		}
	}
	return error;
}

function validarTel() {
	var telf = $('#telf').val().trim();
	var error = "";

	error = validacioCampBuit("telf", "telf_erroni");;

	if (telf.length != 0) {
		var stripped = telf.replace(/[\(\)\.\-\ ]/g, '');

		if (!(stripped.length == 9)) {
			error = "Llargada incorrecta";
			mostrarError("telf_erroni", error);
		} else if (isNaN(stripped)) {
			error = "Caràcters no permesos";
			mostrarError("telf_erroni", error);
		} else {
			eliminarError("telf_erroni");
		}

	}
	return error;
}

//Validar el correu electrònic.
//Apareix el modal d'avis del correu electrònic si inici és 0 i no hi ha cap error
function validarEmail(inici) {
	var error = validacioCampBuit("email", "correu_erroni");
	if (error.length == 0) {
		error = validarCorreu();
      if (error.length == 0 && $('#email_conf').val().trim()!='') correuAdvert(inici);
	}
	return error;
}

//Validar la confirmació del correu electrònic.
//Apareix el modal d'avis del correu electrònic si inici és 0 i no hi ha cap error
function validarEmailConf(inici) {
	var error = validacioCampBuit("email_conf", "correu_conf_erroni");
	if (error.length == 0) {
		error = validarCorreu();
      if (error.length == 0 && $('#email').val().trim()!='' && inici==0) correuAdvert(inici);
	}
	return error;
}

function validarCorreu() {
	var email = $('#email').val().trim();
	var email_conf = $('#email_conf').val().trim();
	var error = "";

	if (email.length != 0 && email_conf.length != 0) {
		var tfld_email = $.trim(email);
		var tfld_email_conf = $.trim(email_conf);
		var emailFilter = /^([\w-\.]+@([\w-]+\.)+[\w-]{2,4})?$/;
		var illegalChars = /[\(\)\<\>\,\;\:\\\"\[\]]/;

		if (!emailFilter.test(tfld_email)) {
			error = "Correu electrònic no vàlid";
			mostrarError("correu_erroni", error);
		} else if (email.match(illegalChars)) {
			error = "Caràcters no permesos";
			mostrarError("correu_erroni", error);
		}

		if (!emailFilter.test(tfld_email_conf)) {
			error = "Correu electrònic no vàlid";
			mostrarError("correu_conf_erroni", error);
		} else if (email_conf.match(illegalChars)) {
			error = "Caràcters no permesos";
			mostrarError("correu_conf_erroni", error);
		}

		if (error == "") {
			if (email != email_conf) {
				error = "No coincideixen els correus electrònics";
				mostrarError("correu_erroni", error);
				mostrarError("correu_conf_erroni", error);
			} else {
				eliminarError("correu_erroni");
			}
		}
	}
	return error;
}
//
// function correuAdvert() {
// 	var email = $('#email').val().trim();
// 	var correu = email.split("@")[1];
//
// 	if ( !$('#modalCorreuValid').hasClass('show') ) {
// 		var correuNoValid = $.ajax({
// 			url: domini + "ajax/buscarCorreusNoValid.php",
// 			method: "GET",
// 			dataType: "html"
// 		});
//
// 		correuNoValid.done(function(esCorreuValid) {
// 			if ( !esCorreuValid.toLowerCase().includes("error") && esCorreuValid != '') {
// 				// Casos (hotmail.es,hotmail.com,yahoo.es|Hotmail,Yahoo!)
// 				var vectEl = esCorreuValid.split(","),
// 					 vectTermAdvert = vectEl[0].split("|"),
// 					 vectNomTermAdvert = vectEl[1].split("|"),
// 					 correuActValid = true,
// 					 vc=0;
// 				 while (vc<vectTermAdvert.length && correuActValid) {
// 					if (vectTermAdvert[vc].toLowerCase() == correu.toLowerCase())
// 						correuActValid=false;
// 					else
// 						vc++;
// 				}
//
// 				if (!correuActValid) {
// 					var terminacions = '';
// 					for (var vc=0; vc<vectNomTermAdvert.length; vc++) {
// 						if (vc>0) {
// 							if (vc==vectNomTermAdvert.length-1)
// 								terminacions += ' i ';
// 							else
// 								terminacions += ', ';
// 						}
// 						terminacions += "<span><em>"+vectNomTermAdvert[vc]+"</em></span> ";
// 					}
// 					var msgInscModal = "<p>Et recomanem que posis un altre correu ";
// 					msgInscModal += "electrònic si el tens, atès que els comptes ";
// 					msgInscModal += "de " + terminacions + " poden no rebre els ";
// 					msgInscModal += "missatges del Campus i de Secretaria.</p>";
// 					msgInscModal += "<p class='mb-0'>Gràcies.</p>";
// 					$("#modalCorreuValidBody").html(msgInscModal);
// 					$('#modalCorreuValid').modal('show');
// 				}
// 			}
// 		});
//
// 		correuNoValid.fail(function(jqXHR, textStatus, errorThrown) {
// 			rerrorFunction(jqXHR, textStatus, errorThrown, "Hi ha hagut algun error a l'hora de comprovar si el correu és vàlid");
// 		});
//
// 	}
// }

function correuAdvert(inici) {
	var email = $('#email').val().trim();
	var correu = email.split("@")[1];
	var error = '';

	esCorreuValid = "hotmail.com|hotmail.es|hotmail.co.uk|outlook.com|outlook.es|live.com|live.es|msn.com|YAHOO.COM|YAHOO.ES|YAHOO.FR,Yahoo!|Hotmail|Outlook|Live|Msn";

	if ( esCorreuValid != '') { //(hotmail.es,hotmail.com,yahoo.es|Hotmail,Yahoo!)
		var vectEl = esCorreuValid.split(","),
			 vectTermAdvert = vectEl[0].split("|"),
			 vectNomTermAdvert = vectEl[1].split("|"),
			 correuActValid = true,
			 vc=0;
		 while (vc<vectTermAdvert.length && correuActValid) {
				if (vectTermAdvert[vc].toLowerCase() == correu.toLowerCase())
				correuActValid=false;
				else
				vc++;
			}

		if (!correuActValid) {
			var terminacions = '';
			for (var vc=0; vc<vectNomTermAdvert.length; vc++) {
				if (vc>0) {
					if (vc==vectNomTermAdvert.length-1)
						terminacions += ' i ';
					else
						terminacions += ', ';
				}
				terminacions += "<span><em>"+vectNomTermAdvert[vc]+"</em></span>";
			}
			var msgInscModal = "Per assegurar-nos que rebràs tota la informació del teu curs sense cap inconvenient, et demanem que <span class='font-weight-bold'>no utilitzis un correu dels comptes " + terminacions + "</span>.";
			if ( !$('#modalCorreuValid').hasClass('show') && !inici) {
				$("#modalCorreuValidBody").html(msgInscModal);
				$('#modalCorreuValid').modal('show');
			}
			error = "Correu electrònic no vàlid";
			mostrarError("correu_erroni", error);
			mostrarError("correu_conf_erroni", error);
		}
	}
	return error;
}

function haveArroba(valor) {
	var arroba = "@";

	for (i = 0; i < valor.length; i++) {
		if (arroba.indexOf(valor.charAt(i), 0) != -1) {
			return 1;
		}
	}
	return 0;
}

function validarAdreca() {
	var adreca = $('#adreca').val().trim();
	var error = validacioCampBuit("adreca", "adreca_erroni");

	if (adreca.length != 0) {
		if (haveArroba(adreca)) {
			error = "Adre\u00E7a postal incorrecta.";
			mostrarError("adreca_erroni", error);
		} else {
			eliminarError("adreca_erroni");
		}
	}
	return error;
}

function validarCP() {
	var cp = $('#cp').val().trim();
	var error = validacioCampBuit("cp", "cp_erroni");

	if (cp.length != 0) {
		var stripped = cp.replace(/[\(\)\.\-\ ]/g, '');

		if (!(stripped.length == 5)) {
			error = "Llargada incorrecta";
			mostrarError("cp_erroni", error);
		} else if (isNaN(parseInt(stripped))) {
			error = "Caràcters no permesos";
			mostrarError("cp_erroni", error);
		} else {
			eliminarError("cp_erroni");
		}

	}
	return error;
}

function haveNumbers(valor) {
	var numeros = "0123456789";
	for (i = 0; i < valor.length; i++) {
		if (numeros.indexOf(valor.charAt(i), 0) != -1) {
			return true;
		}
	}
	return false;
}

function validarPoble() {
	var poble = $('#poble').val().trim();

	var error = validacioCampBuit("poble", "poble_erroni");

	if (poble.length != 0) {
		if ((haveNumbers(poble))) {
			error = "Poblacic&oacute incorrecta";
			mostrarError("poble_erroni", error);
		} else {
			eliminarError("poble_erroni");
		}
	}
	return error;
}

function esAlphabetic(valor) {
	var re = /^[a-zA-Z]+$/;
	if (re.test(valor)) return true
	else return false
}

function esNumeric(valor) {
	var log = valor.length;
	var sw = "S";
	for (x = 0; x < log - 1; x++) {
		v1 = valor.substr(x, 1);
		v2 = parseInt(v1);
		if (isNaN(v2)) sw = "N";
	}

	if (sw == "S") return true
	else return false
}

function validarPass() {
	return validacioCampBuit("pass", "dni_erroni");
}


//Si el input amb id "id" està buit, mostra l'error "Camp obligatori" a l'id id_error
function validacioCampBuit(id, id_error) {
	var error = "";

	if ($("#" + id).val().trim().length == 0) {
		error = "Camp obligatori";
		mostrarError(id_error, error);
	} else {
		eliminarError(id_error);
	}
	return error;
}

//Mostra l'error error a l'id id_error
function mostrarError(id_error, error) {
	$("#" + id_error).html(error);
	$("#" + id_error).addClass("erroni");
}

//Elimina l'error de l'id id_error
function eliminarError(id_error) {
	$("#" + id_error).html("");
	$("#" + id_error).removeClass("erroni");
}

function mostrarModalError(missatgeError) {
	$('#modalErrorsBody').html(missatgeError);
	$('#modalErrors').modal('show');
}

/* ############################### GENERAL ############################### */
function adjustStyle() {
	screenWidth = parseInt($(this).width());

	if (screenWidth < 575) {
		$('.prisma-footer .panel').css('display', 'none');
		$('.accordion-footer').removeClass('active');
		$('.accordion-footer').click(function() {
			this.classList.toggle("active");
			var panel = this.nextElementSibling;
			if (panel.style.display === "block") {
				panel.style.display = "none";
				panel.style.maxHeight = null
			} else {
				panel.style.display = "block";
				panel.style.maxHeight = panel.scrollHeight + "px"
			}
		})
	}
	else {
		$('.accordion-footer').removeClass('active');
		$('.prisma-footer .panel').css('display', 'block')
	}
}

function afegirCSS() {
	$('body').append("<link rel='stylesheet' href='https://fonts.googleapis.com/css?family=Roboto:100,100i,300,400,400i,500,500i,700,700i|Nunito+Sans&display=swap' />");
	$('body').append("<link rel='stylesheet' href='" + domini + "css1619773569/font-awesome-prisma.min.css' />");
	$('body').append("<link rel='shortcut icon' type='image/x-icon' href='https://www.prisma.cat/favicon.ico'/>");
	$('body').append("<link rel='stylesheet' href='" + domini + "css1619773569/404.min.css' />")
}

afegirCSS();

function openNav() {
   $(".navbarPrisma").css('width', '270px');
   $('.closebtn').css('display', 'block');
   $('.navBarPrisma').addClass('w-100');
   $("#pagina").addClass('sideNavBarPrismaObert');
   $("#pagina > .container").addClass('sideNavBarContainerPrismaObert');
   $('body').css('overflow-y', 'hidden')
}

function closeNav() {
   $(".navbarPrisma").css('width', '0');
   $('.closebtn').css('display', 'none');
   $('.navBarPrisma').removeClass('w-100');
   $("#pagina").removeClass('sideNavBarPrismaObert');
   $("#pagina > .container").removeClass('sideNavBarContainerPrismaObert');
   $('body').css('overflow-y', 'auto')
}
window.onscroll = function() {
   noPerdreHeader()
};

function noPerdreHeader() {
   if (document.body.scrollTop > 0 || document.documentElement.scrollTop > 0) {
      $('#top-menu').css('display', 'none');
      $('#nav-header').css('position', 'fixed')
   } else {
      if (!(/Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent)))
         $('#top-menu').css('display', 'block');
      $('#nav-header').css('position', 'inherit')
   }
}
